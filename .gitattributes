# Windows batch files MUST be CRLF — cmd.exe cannot run LF-only batch files.
*.bat text eol=crlf
*.cmd text eol=crlf

# Text files normalize to LF in the repo (checkout CRLF on Windows by default).
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.ini text eol=lf
